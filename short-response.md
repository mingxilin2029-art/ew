1. first need to do the git branch because need to check what branch classmate make before.
2. developers use branches to know the what team mate make before and it is eazy on the other city to work together.
3. github is where repository live git is atool that we use for version control git work inside the github 
4. git add . and git commit -m "saving work" is replace the git add . on the top git commit -m "saving work" on the middle git push on the bottom